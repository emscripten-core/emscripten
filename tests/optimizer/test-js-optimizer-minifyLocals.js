function main(x) {
 constructor(); // special word in JS, must not be confused by it
 x(); // this should assert!
}
// EMSCRIPTEN_GENERATED_FUNCTIONS
// EXTRA_INFO: { "globals": { "main": "a", "constructor": "b" } }
