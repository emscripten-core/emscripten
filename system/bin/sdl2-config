#!/bin/sh

echo "emscripten sdl2-config called with $*" >&2

for arg in "$@"; do
  case "$arg" in
    --cflags|--libs)
      echo "-sUSE_SDL=2"
      ;;
    --version)
      echo "2.0.10"
      ;;
  esac
done

